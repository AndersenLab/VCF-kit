language: python
python:
  - "2.7"

install: 
  - sudo pip install cython
  - pip install -r requirements.txt
  - python setup.py install
addons:
  apt:
    packages:
    - bwa
    - samtools

script: 
  - py.test test.py

sudo: true